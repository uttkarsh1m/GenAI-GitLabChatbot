# Environment
.env
*.env

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
venv/
env/
.venv/

# Data files — commit these for deployment (needed by the app)
# Only ignore the raw scraped content (large, regeneratable)
data/gitlab_content.json

# Keep the index and chunks — required for the app to run
# data/faiss_index.bin   ← DO NOT ignore, needed for deployment
# data/chunks.json       ← DO NOT ignore, needed for deployment

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Streamlit
.streamlit/secrets.toml
